<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>CognitoSample</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
  <meta name="AppInfo" content="Our app info"
                         data-aws-region="<%= ENV['AWS_REGION'] %>"
                         data-cognito-identity-pool-id="<%= ENV['COGNITO_IDENTITY_POOL_ID'] %>"
                         data-cognito-user-pool-id="<%= ENV['COGNITO_USER_POOL_ID'] %>"
                         data-cognito-client-id="<%= ENV['COGNITO_CLIENT_ID'] %>">
</head>
<body>

  <nav class="navbar navbar-default">
    <div class="container-fluid">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbarExample">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="/">Cognito Sample</a>
      </div>

      <div class="collapse navbar-collapse" id="navbarExample">
        <div id="login_and_signup_menus">
          <ul class="nav navbar-nav">
            <li id="menu_user_login"><%= link_to 'Login', login_path %></li>
            <li id="menu_user_signup"><%= link_to 'Signup', signup_path %></li>
          </ul>
        </div>
        <ul class="nav navbar-nav" id="logged_in_menus" style="display:none">
          <li><a id="user_logout_btn" href="#" data-confirm="Logout ?">Logout</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container">
<%= yield %>
  </div>

</body>
</html>
